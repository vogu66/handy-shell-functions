#!/bin/bash

source ~/.functions

ask()
{
	echo
	read -p "Do you want to start $1?" -n 1 -r
	echo

	if [[ $REPLY =~ ^[Yy]$ ]]
	then
		detach $@
		return 0
	fi
}

internet=$(ask nm-applet);
echo
if (! $internet)
then
	
	ask discord
	ask caprine
	ask skype

fi

ask redshift
ask shalarm 12:00